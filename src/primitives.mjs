import primitives from './primitives.js';

export const {
  inRange,
  atLeast,
  nonNegInt,
  noopLogger,
  positiveInt,
  applyOptions,
  nonNegFinite,
  consoleLogger,
  validHashAlgo,
  settleOnEvents,
  positiveFinite,
  normalizeLogger,
  positiveIntOrInfinity,
} = primitives;

export default primitives;